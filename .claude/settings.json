{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "shell": "bash",
            "command": "cd \"$CLAUDE_PROJECT_DIR\" && git add -A && (git diff --cached --quiet || git commit -q -m \"Auto-commit: $(date '+%Y-%m-%d %H:%M')\") && git push -q origin main 2>&1 || true",
            "timeout": 90,
            "statusMessage": "Syncing to GitHub..."
          }
        ]
      }
    ]
  }
}
